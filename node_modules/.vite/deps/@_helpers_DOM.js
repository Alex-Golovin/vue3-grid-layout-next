// src/helpers/DOM.ts
var currentDir = "auto";
function hasDocument() {
  return typeof document !== "undefined";
}
function hasWindow() {
  return typeof window !== "undefined";
}
function getDocumentDir() {
  if (!hasDocument()) {
    return currentDir;
  }
  const direction = typeof document.dir !== "undefined" ? document.dir : document.getElementsByTagName("html")[0].getAttribute("dir") || "auto";
  return direction;
}
function setDocumentDir(dir) {
  if (!hasDocument) {
    currentDir = dir;
    return false;
  }
  const html = document.getElementsByTagName("html")[0];
  html.setAttribute("dir", dir);
  return true;
}
function addWindowEventListener(event, callback) {
  if (!hasWindow) {
    callback();
    return false;
  }
  window.addEventListener(event, callback);
  return true;
}
function removeWindowEventListener(event, callback) {
  if (!hasWindow) {
    return;
  }
  window.removeEventListener(event, callback);
}
export {
  addWindowEventListener,
  getDocumentDir,
  removeWindowEventListener,
  setDocumentDir
};
//# sourceMappingURL=@_helpers_DOM.js.map
